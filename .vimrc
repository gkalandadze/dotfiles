" Use ctrl+c and ctrl +v to copy and paste from clipboard
vnoremap <C-c> "+y
map <C-v> "+P

" Disable compatibility with vi which can cause unexpected issues.
set nocompatible

" Encoding
set encoding=utf-8

" Turn on syntax highlighting.
syntax on

" Show line numbers
set number

" Use space characters instead of tabs.
set expandtab

" Set tab width to 4 columns.
set tabstop=4
